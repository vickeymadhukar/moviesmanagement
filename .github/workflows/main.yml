name: Deploy Movies List App on EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # =========================
      # Backend
      # =========================

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: npm install

      - name: Create Backend .env
        working-directory: ./backend
        run: |
          echo "${{ secrets.BACKEND_ENV }}" > .env

      - name: Start Backend with PM2
        working-directory: ./backend
        run: |
          /home/ubuntu/.nvm/versions/node/v24.19.0/bin/pm2 restart movies-list-api --update-env || /home/ubuntu/.nvm/versions/node/v24.19.0/bin/pm2 start server.js --name movies-list-api
          /home/ubuntu/.nvm/versions/node/v24.19.0/bin/pm2 save

      # =========================
      # Frontend
      # =========================

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm install

      - name: Create Frontend .env
        working-directory: ./frontend
        run: |
          echo "${{ secrets.FRONTEND_ENV }}" > .env

      - name: Build Frontend
        working-directory: ./frontend
        run: npm run build

      # =========================
      # Deploy to Nginx
      # =========================

      - name: Deploy Frontend to Nginx
        run: |
          sudo rm -rf /var/www/html/*
          sudo cp -r frontend/dist/* /var/www/html/

      - name: Reload Nginx
        run: |
          sudo systemctl reload nginx
